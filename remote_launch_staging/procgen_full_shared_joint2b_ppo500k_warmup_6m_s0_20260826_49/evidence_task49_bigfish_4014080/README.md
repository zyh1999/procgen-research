# Task49 BigFish exact-4M bounded archive

This model-free archive preserves BigFish job `1074926`: exact-2M PASS
followed by exact-4M `EARLY_STOPPED_ALGORITHM`. Complete source traces and
logs remain immutable at the recorded Bede root. The Git archive contains the
complete progress table, bounded terminal trace/log snapshots, phase switch,
frozen identity, exact ledger, scheduler reconciliation and hashes.

Models and checkpoints are excluded.
