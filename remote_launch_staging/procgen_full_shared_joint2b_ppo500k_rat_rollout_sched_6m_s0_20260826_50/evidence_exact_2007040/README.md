# Task50 exact-2M bounded archive

This model-free archive preserves the two Task50 exact-stage algorithm early
stops at transition `2,007,040`. Complete source traces and logs remain at
the immutable Bede roots; the Git archive includes exact ledgers, complete
progress and rollout-scheduler tables, bounded terminal trace/log snapshots,
phase-switch records, frozen identities, scheduler reconciliation and hashes.

Models and checkpoints are excluded.
